const BaseModel = require('./baseModel')
const db = require('../config/database')

const Helpers = require('../utils/helpers')

class StudyModel extends BaseModel {
    constructor() {
        super('studies');
    }

    //Get studies with patient and order details
    
}